# docker compose 用の環境変数サンプル。
# 利用時は `cp .env.example .env` でコピーして値を調整する（.env は git 管理外）。

# PostgreSQL
POSTGRES_USER=rinshouji
POSTGRES_PASSWORD=rinshouji
POSTGRES_DB=rinshouji

# ホスト側に公開するポート
FRONTEND_PORT=8123
BACKEND_PORT=3000
DB_PORT=5432
STORYBOOK_PORT=8124
